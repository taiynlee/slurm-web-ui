[project]
name = "backend"
version = "0.1.0"
description = "Slurm WebUI backend — unauthenticated Slurm REST API proxy with TTL cache"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cachetools>=7.0.6",
    "fastapi>=0.136.0",
    "httpx>=0.28.1",
    "pydantic-settings>=2.14.0",
    "uvicorn[standard]>=0.46.0",
]
