apiVersion: v1
kind: Service
metadata:
  name: slurm-webui
  namespace: default
spec:
  selector:
    app: slurm-webui
  type: ClusterIP   # 改成 NodePort 或 LoadBalancer 可對外開放
  ports:
    - name: http
      port: 80
      targetPort: 8000
